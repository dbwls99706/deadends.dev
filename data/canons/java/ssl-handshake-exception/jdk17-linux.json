{
  "schema_version": "1.0.0",
  "id": "java/ssl-handshake-exception/jdk17-linux",
  "url": "https://deadends.dev/java/ssl-handshake-exception/jdk17-linux",
  "error": {
    "signature": "javax.net.ssl.SSLHandshakeException",
    "regex": "javax\\.net\\.ssl\\.SSLHandshakeException",
    "domain": "java",
    "category": "runtime_error",
    "first_seen": "2023-01-01",
    "last_confirmed": "2026-02-12"
  },
  "environment": {
    "runtime": {
      "name": "java",
      "version_range": ">=17,<21"
    },
    "os": "linux"
  },
  "verdict": {
    "resolvable": "true",
    "fix_success_rate": 0.82,
    "confidence": 0.85,
    "last_updated": "2026-02-12",
    "summary": "SSLHandshakeException occurs when the TLS/SSL handshake between client and server fails. Common causes: untrusted certificate (self-signed or expired), certificate hostname mismatch, incompatible TLS versions, or missing CA certificates in the Java truststore."
  },
  "dead_ends": [
    {
      "action": "Disabling SSL/TLS certificate verification entirely with a TrustManager that trusts all certificates",
      "why_fails": "A TrustManager that accepts all certificates disables the fundamental security guarantee of TLS. This makes the application vulnerable to man-in-the-middle attacks where an attacker can intercept and modify all encrypted traffic. This is never acceptable in production and should be avoided even in development.",
      "fail_rate": 0.3,
      "sources": [
        "https://docs.oracle.com/en/java/javase/21/docs/api/"
      ],
      "condition": ""
    },
    {
      "action": "Downgrading TLS version to TLSv1.0 or TLSv1.1 to avoid handshake failures",
      "why_fails": "TLSv1.0 and TLSv1.1 have known security vulnerabilities and are disabled by default in JDK 17+. Downgrading TLS version opens the connection to BEAST, POODLE, and other attacks. Modern servers increasingly reject TLSv1.0/1.1 connections entirely.",
      "fail_rate": 0.7,
      "sources": [
        "https://docs.oracle.com/en/java/javase/21/docs/api/"
      ],
      "condition": ""
    }
  ],
  "workarounds": [
    {
      "action": "Import the server's certificate or CA certificate into the Java truststore",
      "success_rate": 0.88,
      "how": "1. Download the server certificate: 'openssl s_client -connect host:443 -showcerts'. 2. Save the certificate to a file (PEM format). 3. Import into the Java truststore: 'keytool -importcert -alias myserver -file server.crt -keystore $JAVA_HOME/lib/security/cacerts -storepass changeit'. 4. Alternatively, create a custom truststore and pass it via -Djavax.net.ssl.trustStore=/path/to/truststore.jks. 5. For Spring Boot: configure server.ssl.trust-store properties.",
      "sources": [],
      "condition": "when the server uses a self-signed or internal CA certificate"
    },
    {
      "action": "Update the JDK's CA certificates and ensure the server certificate is valid and not expired",
      "success_rate": 0.85,
      "how": "1. Check certificate validity: 'openssl s_client -connect host:443 | openssl x509 -noout -dates'. 2. Verify the certificate chain is complete — the server must send all intermediate certificates. 3. Update the JDK to the latest patch version, which includes updated CA certificates. 4. If the server certificate's CN or SAN does not match the hostname, fix the server configuration or use the correct hostname. 5. For TLS version issues: check which TLS versions the server supports with 'nmap --script ssl-enum-ciphers -p 443 host'.",
      "sources": [],
      "condition": "when the server uses a public CA certificate"
    }
  ],
  "transition_graph": {
    "leads_to": [
      {
        "error_id": "java/socketexception-connection-reset/jdk17-linux",
        "probability": 0.15,
        "condition": "when TLS fix changes cipher suites and causes connection resets with incompatible servers"
      },
      {
        "error_id": "java/jdbc-connection-refused/jdk17-linux",
        "probability": 0.1,
        "condition": "when database SSL configuration change causes connection failures"
      }
    ],
    "preceded_by": [
      {
        "error_id": "java/jdbc-connection-refused/jdk17-linux",
        "probability": 0.12,
        "condition": "when enabling SSL on a JDBC connection that previously used plaintext"
      },
      {
        "error_id": "java/socketexception-connection-reset/jdk17-linux",
        "probability": 0.08,
        "condition": "when investigating connection resets reveals underlying TLS negotiation failures"
      }
    ],
    "frequently_confused_with": [
      {
        "error_id": "java/socketexception-connection-reset/jdk17-linux",
        "distinction": "SSLHandshakeException is specifically about TLS certificate/protocol negotiation failure; SocketException: Connection reset is about TCP-level abrupt closure. Both can occur during connection establishment but have different root causes and fixes."
      }
    ]
  },
  "metadata": {
    "generated_by": "bulk_generate.py",
    "generation_date": "2026-02-12",
    "review_status": "auto_generated",
    "evidence_count": 70,
    "last_verification": "2026-02-12"
  }
}
