{
  "schema_version": "1.0.0",
  "id": "networking/connection-refused/tcp-linux",
  "url": "https://deadends.dev/networking/connection-refused/tcp-linux",
  "error": {
    "signature": "Connection refused (ECONNREFUSED)",
    "regex": "Connection refused|ECONNREFUSED|connect.*refused|errno 111",
    "domain": "networking",
    "category": "tcp_connection",
    "first_seen": "2023-01-01",
    "last_confirmed": "2026-02-12"
  },
  "environment": {
    "runtime": {
      "name": "tcp",
      "version_range": "any"
    },
    "os": "linux"
  },
  "verdict": {
    "resolvable": "true",
    "fix_success_rate": 0.92,
    "confidence": 0.91,
    "last_updated": "2026-02-12",
    "summary": "The target host actively refused the TCP connection. This means the host is reachable but no service is listening on the requested port. The OS sends a TCP RST packet in response to the SYN."
  },
  "dead_ends": [
    {
      "action": "Increase connection timeout hoping the service will eventually start listening",
      "why_fails": "Connection refused is an immediate, active rejection (RST packet), not a timeout. The OS immediately tells the client that nothing is listening. Increasing timeout has no effect because the error occurs instantly, not after a delay.",
      "fail_rate": 0.95,
      "sources": [
        "https://man7.org/linux/man-pages/man2/connect.2.html"
      ],
      "condition": ""
    },
    {
      "action": "Change the client to use a different network interface or source IP",
      "why_fails": "ECONNREFUSED means the target host received the SYN packet and responded with RST because no process is listening on that port. The issue is on the server side, not the network path. Changing source IP or interface does not affect what is listening on the server.",
      "fail_rate": 0.9,
      "sources": [
        "https://man7.org/linux/man-pages/man2/connect.2.html"
      ],
      "condition": ""
    }
  ],
  "workarounds": [
    {
      "action": "Start or restart the target service on the correct port",
      "success_rate": 0.93,
      "how": "1. Verify the service status: systemctl status your-service\n2. Check if something is listening on the expected port: ss -tlnp | grep :PORT\n3. Start the service: systemctl start your-service\n4. Check logs for startup failures: journalctl -u your-service --no-pager -n 50\n5. Verify it is now listening: ss -tlnp | grep :PORT\n6. Test connection: nc -zv host PORT",
      "sources": [],
      "condition": ""
    },
    {
      "action": "Verify the correct host and port configuration on the client",
      "success_rate": 0.88,
      "how": "1. Confirm the expected host:port from documentation or configuration\n2. Verify DNS resolution: dig hostname +short\n3. Check if the service listens on a different port: ss -tlnp on the server\n4. Check if the service binds to 127.0.0.1 vs 0.0.0.0: ss -tlnp | grep :PORT\n5. If bound to localhost only, reconfigure to bind to 0.0.0.0 or the correct interface\n6. Check firewall rules are not redirecting traffic: iptables -t nat -L",
      "sources": [],
      "condition": ""
    }
  ],
  "transition_graph": {
    "leads_to": [
      {
        "error_id": "networking/connection-timeout/tcp-linux",
        "probability": 0.15,
        "condition": "when the service starts but a firewall is then enabled that drops packets instead of rejecting"
      },
      {
        "error_id": "networking/address-already-in-use/tcp-linux",
        "probability": 0.2,
        "condition": "when attempting to restart the service but the old process still holds the port"
      }
    ],
    "preceded_by": [
      {
        "error_id": "networking/dns-resolution-failed/dns-linux",
        "probability": 0.15,
        "condition": "when DNS is fixed and the host resolves but the service is not yet running"
      },
      {
        "error_id": "networking/address-already-in-use/tcp-linux",
        "probability": 0.1,
        "condition": "when EADDRINUSE prevented the service from starting, resulting in refused connections from clients"
      }
    ],
    "frequently_confused_with": [
      {
        "error_id": "networking/connection-timeout/tcp-linux",
        "distinction": "Connection refused is an immediate RST response meaning the host is up but no service listens; timeout means no response at all, typically due to firewall drops or host being down"
      },
      {
        "error_id": "networking/host-unreachable/tcp-linux",
        "distinction": "Connection refused means the host was reached and actively rejected; host unreachable means no network route exists to the host at all"
      }
    ]
  },
  "metadata": {
    "generated_by": "bulk_generate.py",
    "generation_date": "2026-02-12",
    "review_status": "auto_generated",
    "evidence_count": 80,
    "last_verification": "2026-02-12"
  }
}
