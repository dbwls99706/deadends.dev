{
  "schema_version": "1.0.0",
  "id": "ros2/message-type-hash-mismatch-rebuild/humble-linux",
  "url": "https://deadends.dev/ros2/message-type-hash-mismatch-rebuild/humble-linux",
  "error": {
    "signature": "ROS2 'type hash mismatch' or 'incompatible type' error after modifying custom message definition",
    "regex": "(type hash mismatch|incompatible.*type|message.*definition.*changed|TypeHash.*differ|IDL.*mismatch)",
    "domain": "ros2",
    "category": "build_system",
    "first_seen": "2023-01-01",
    "last_confirmed": "2026-02-24"
  },
  "environment": {
    "runtime": {
      "name": "ros2",
      "version_range": "humble"
    },
    "os": "linux"
  },
  "verdict": {
    "resolvable": "true",
    "fix_success_rate": 0.88,
    "confidence": 0.9,
    "last_updated": "2026-02-24",
    "summary": "After modifying a .msg/.srv/.action file, the type hash changes. Running nodes using the old compiled message type cannot communicate with nodes using the new type. Clean rebuild of ALL dependent packages is required, not just the message package."
  },
  "dead_ends": [
    {
      "action": "Rebuild only the message package after changing .msg definition",
      "why_fails": "All packages that depend on the message must also be rebuilt. Their compiled bindings reference the old type hash. Partial rebuild = type hash mismatch at runtime.",
      "fail_rate": 0.9,
      "sources": [],
      "condition": ""
    },
    {
      "action": "Restart nodes without rebuilding after .msg change",
      "why_fails": "Compiled message types are baked into the binaries. Restarting without rebuilding uses the same old binaries with old type hashes.",
      "fail_rate": 0.88,
      "sources": [],
      "condition": ""
    }
  ],
  "workarounds": [
    {
      "action": "Clean rebuild all dependent packages after message changes",
      "success_rate": 0.95,
      "how": "colcon build --packages-above my_msgs  # rebuilds my_msgs AND all packages that depend on it",
      "sources": [],
      "condition": ""
    },
    {
      "action": "Delete build/ and install/ directories for a guaranteed clean state",
      "success_rate": 0.88,
      "how": "rm -rf build/ install/ log/ && colcon build  # nuclear option but guarantees no stale artifacts",
      "sources": [],
      "condition": ""
    },
    {
      "action": "Source install/setup.bash in ALL terminals after rebuild",
      "success_rate": 0.85,
      "how": "source install/setup.bash  # every terminal running ROS2 nodes must re-source after message type changes",
      "sources": [],
      "condition": ""
    }
  ],
  "transition_graph": {
    "leads_to": [],
    "preceded_by": [],
    "frequently_confused_with": []
  },
  "metadata": {
    "generated_by": "bulk_generate.py",
    "generation_date": "2026-02-24",
    "review_status": "auto_generated",
    "evidence_count": 50,
    "last_verification": "2026-02-24"
  }
}
