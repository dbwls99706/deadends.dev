{
  "schema_version": "1.0.0",
  "id": "security/cors-wildcard-with-credentials/cors-browser-any",
  "url": "https://deadends.dev/security/cors-wildcard-with-credentials/cors-browser-any",
  "error": {
    "signature": "CORS request with credentials fails: 'Access-Control-Allow-Origin' cannot be wildcard when credentials flag is true",
    "regex": "(Access-Control-Allow-Origin.*wildcard.*credentials|CORS.*wildcard.*cookie|credentials.*not.*supported.*wildcard)",
    "domain": "security",
    "category": "cors",
    "first_seen": "2023-01-01",
    "last_confirmed": "2026-02-24"
  },
  "environment": {
    "runtime": {
      "name": "cors-browser",
      "version_range": ">=any"
    },
    "os": "any"
  },
  "verdict": {
    "resolvable": "true",
    "fix_success_rate": 0.92,
    "confidence": 0.95,
    "last_updated": "2026-02-24",
    "summary": "When a request includes credentials (cookies, auth headers), the CORS response MUST specify the exact origin, not '*'. Browsers reject wildcard Allow-Origin with credentials. This catches nearly every developer setting up CORS for the first time."
  },
  "dead_ends": [
    {
      "action": "Set Access-Control-Allow-Origin: * and Access-Control-Allow-Credentials: true",
      "why_fails": "Browsers explicitly reject this combination. The spec forbids wildcard origin with credentials to prevent credential leaking to arbitrary origins.",
      "fail_rate": 0.95,
      "sources": [
        "https://owasp.org/www-project-top-ten/"
      ],
      "condition": ""
    },
    {
      "action": "Remove Allow-Credentials header to make wildcard work",
      "why_fails": "Without credentials, cookies and auth headers are not sent. Authenticated API calls fail silently with 401.",
      "fail_rate": 0.85,
      "sources": [
        "https://owasp.org/www-project-top-ten/"
      ],
      "condition": ""
    }
  ],
  "workarounds": [
    {
      "action": "Echo the request Origin header back as Allow-Origin after validating it",
      "success_rate": 0.95,
      "how": "origin = request.headers['Origin']; if origin in ALLOWED_ORIGINS: response.headers['Access-Control-Allow-Origin'] = origin",
      "sources": [],
      "condition": ""
    },
    {
      "action": "Always include Vary: Origin when reflecting origin to prevent CDN cache poisoning",
      "success_rate": 0.92,
      "how": "response.headers['Vary'] = 'Origin'  # critical if response passes through CDN or cache",
      "sources": [],
      "condition": ""
    },
    {
      "action": "Use a CORS library that handles origin validation correctly",
      "success_rate": 0.88,
      "how": "Flask-CORS, django-cors-headers, cors (Express) - all handle origin reflection and Vary header automatically",
      "sources": [],
      "condition": ""
    }
  ],
  "transition_graph": {
    "leads_to": [],
    "preceded_by": [],
    "frequently_confused_with": []
  },
  "metadata": {
    "generated_by": "bulk_generate.py",
    "generation_date": "2026-02-24",
    "review_status": "auto_generated",
    "evidence_count": 3,
    "last_verification": "2026-02-24"
  }
}
